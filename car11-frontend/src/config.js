// Configuration for API endpoints
const config = {
  // In production, API calls will be relative to the same domain
  // In development, they will proxy through Vite dev server
  API_BASE_URL: '',
  API_PREFIX: '/api'
}

export default config

